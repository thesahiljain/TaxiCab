import React from 'react';
import { Text, Image, StyleSheet } from 'react-native';
import { View, Button } from 'native-base';

const DriverFoundComponent = () => {
    return (
        <View>
            
        </View>
    );
}

const styles = StyleSheet.create({

});

export default DriverFoundComponent;